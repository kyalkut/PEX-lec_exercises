#include <stdio.h>

int main() {
    // to declare pointer og type int *
    int *pointer_v1;
    int *pointer_v2;
    int *pointer_v3;

    // Asterix placement can affect interpretation

    int *p, i; // p is a pointer, i is an int
    int *p1, p2; // both p1 and p2 are pointers
}